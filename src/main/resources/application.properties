spring.application.name=foro_backend

server.port=8080

# Directorio de persistencia JSON (relativo al working directory)
app.data.dir=./data

# JWT — cambiar secret en producción
app.jwt.secret=forumhub-dev-secret-change-in-production-min-256-bits!!
app.jwt.expiration-ms=86400000

# Niveles máximos de respuesta por pregunta: 3, 5 o ilimitado
app.forum.max-reply-levels=3

server.address=0.0.0.0
